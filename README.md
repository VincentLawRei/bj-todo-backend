# Express.js + JWT Authentication + Postgres + Sequelize + TODO CRUD

Classic TODO list.

## Run the Application

To run this application, you need to install Node.js on your machine.

## Install the dependencies:

```
npm install
```

## Start the application:

```
npm start
```
